---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Hero from '../components/Hero.astro';
import ToolCard from '../components/ToolCard.astro';
import TikTokView from '../components/TikTokView.astro';
import { getCollection } from 'astro:content';

const allTools = await getCollection('tools');

const featuredTools = allTools.filter(t => t.data.featured);

// Latest additions: recent tools, prefer ones with thumbnails, randomized
const sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000;
const recentTools = allTools
  .filter(t => new Date(t.data.date_added).getTime() >= sevenDaysAgo)
  .sort((a, b) => {
    // Thumbnails first, then random within each group
    const aHas = a.data.thumbnail ? 0 : 1;
    const bHas = b.data.thumbnail ? 0 : 1;
    return aHas - bHas || Math.random() - 0.5;
  });
// Fall back to all tools sorted by date if not enough recent ones
const pool = recentTools.length >= 9 ? recentTools : allTools
  .sort((a, b) => {
    const aHas = a.data.thumbnail ? 0 : 1;
    const bHas = b.data.thumbnail ? 0 : 1;
    return aHas - bHas || new Date(b.data.date_added).getTime() - new Date(a.data.date_added).getTime();
  });
const latestTools = pool.slice(0, 9);
---

<BaseLayout>
  <Header />
  <Hero />

  <main id="main" class="container">
    <!-- Featured Tools -->
    {featuredTools.length > 0 && (
      <section class="section">
        <div class="section-header">
          <h2 class="section-title">⭐ Featured Tools</h2>
          <p class="section-subtitle">The best of the tiny</p>
        </div>
        <div class="tool-grid featured-grid">
          {featuredTools.map(tool => (
            <ToolCard
              name={tool.data.name}
              slug={tool.id.replace(/\.md$/, '')}
              tagline={tool.data.tagline}
              author={tool.data.author}
              author_github={tool.data.author_github}
              tags={tool.data.tags}
              language={tool.data.language}
              featured={tool.data.featured}
              thumbnail={tool.data.thumbnail}
              github_url={tool.data.github_url}
            />
          ))}
        </div>
      </section>
    )}

    <!-- Latest Tools -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">🆕 Fresh in Town</h2>
        <a href="/tools/" class="view-all">View all →</a>
      </div>
      <div class="tool-grid">
        {latestTools.map(tool => (
          <ToolCard
            name={tool.data.name}
            slug={tool.id.replace(/\.md$/, '')}
            tagline={tool.data.tagline}
            author={tool.data.author}
            author_github={tool.data.author_github}
            tags={tool.data.tags}
            language={tool.data.language}
            featured={tool.data.featured}
            thumbnail={tool.data.thumbnail}
            github_url={tool.data.github_url}
          />
        ))}
      </div>
    </section>

    <!-- What is this? -->
    <section class="section manifesto-section">
      <div class="manifesto-card">
        <h2>What is this place? 🤔</h2>
        <p>
          Tiny Tool Town is a community showcase for <strong>free, fun, and open source tools</strong>.
          These are the tools that are stupid and delightful — made for an audience of one,
          built over a weekend, and probably wouldn't cost more than a buck or two.
        </p>
        <p>
          Think of it as a neighborhood of weird little software shops,
          each one built with love by someone who just wanted to make something fun.
        </p>
        <div class="manifesto-cta">
          <a href="/submit/" class="btn btn-primary">Add Your Tool ✨</a>
          <a href="/about/" class="btn btn-secondary">Learn More</a>
        </div>
      </div>
    </section>
  </main>

  <!-- Floating button to launch Shorts mode -->
  <button id="launch-vertical-view" class="floating-shorts-btn" aria-label="Open shorts view">
    📱 Shorts
  </button>

  <TikTokView tools={allTools} />

  <Footer />
</BaseLayout>

<style>
  .section {
    margin-bottom: 3rem;
  }

  .section-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 1.5rem;
  }

  .section-title {
    font-size: 1.5rem;
    font-weight: 800;
  }

  .section-subtitle {
    color: var(--color-text-muted);
    font-size: 0.95rem;
    margin-left: 0.75rem;
  }

  .view-all {
    color: var(--color-primary);
    font-weight: 600;
    font-size: 0.95rem;
  }

  .tool-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1.5rem;
  }

  .featured-grid {
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  }

  .manifesto-section {
    margin-top: 2rem;
  }

  .manifesto-card {
    background: var(--color-bg-card);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: 2.5rem;
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .manifesto-card h2 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .manifesto-card p {
    color: var(--color-text-muted);
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .manifesto-card strong {
    color: var(--color-primary);
  }

  .manifesto-cta {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-top: 1.5rem;
    flex-wrap: wrap;
  }

  @media (max-width: 640px) {
    .section-header { flex-direction: column; gap: 0.25rem; }
  }

  /* Floating Shorts Button */
  .floating-shorts-btn {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    z-index: 1000;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: #fff;
    border: none;
    padding: 0.875rem 1.5rem;
    border-radius: 50px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 8px 24px rgba(255, 137, 6, 0.35);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .floating-shorts-btn:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 12px 32px rgba(255, 137, 6, 0.5);
  }

  .floating-shorts-btn:active {
    transform: translateY(-1px) scale(1.02);
  }

  @media (max-width: 640px) {
    .floating-shorts-btn {
      bottom: 1.25rem;
      right: 1.25rem;
      padding: 0.75rem 1.25rem;
      font-size: 0.9rem;
    }
  }
</style>

<script>
  // Launch vertical shorts view when button is clicked
  const launchBtn = document.getElementById('launch-vertical-view');
  if (launchBtn) {
    launchBtn.addEventListener('click', () => {
      if (typeof (window as any).launchShortsViewer === 'function') {
        (window as any).launchShortsViewer();
      }
    });
  }
</script>
